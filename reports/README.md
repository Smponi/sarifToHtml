# Reports

This directory stores SARIF reports generated from the fixture projects.

Reports are useful for manual QA, compatibility checks, and developing renderer
behavior against realistic scanner output. Regenerate them with:

```sh
scripts/generate-sarif-fixtures.sh all
```
