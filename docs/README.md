# Documentation

This directory contains project documentation for users and contributors.

The static HTML pages explain the architecture, CLI, SARIF mapping, testing
strategy, and open-source readiness. `tested-tools.md` is a living compatibility
matrix for real scanner output.

Keep these docs in sync with the CLI flags, supported SARIF fields, fixture
workflow, and release-readiness notes in the root `README.md`.
