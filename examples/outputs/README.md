# Example Outputs

This directory contains checked-in HTML outputs that are useful for quick visual
inspection or documentation references. Regenerate outputs from the repository
root so relative paths in commands stay predictable.

