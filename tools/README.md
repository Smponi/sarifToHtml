# Tools

This directory contains helper tooling that supports development workflows but is
not part of the shipped CLI.

Tooling should be reproducible and documented so contributors can regenerate
fixtures and validate reports without reverse-engineering local setup.
