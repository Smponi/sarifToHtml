# SARIF Fixture Tooling

This directory contains the Docker setup used to generate SARIF fixture reports
without requiring every scanner CLI to be installed locally.

Use the root script for normal workflows:

```sh
scripts/generate-sarif-fixtures.sh all
```
