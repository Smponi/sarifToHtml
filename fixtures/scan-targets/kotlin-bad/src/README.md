# Kotlin Source Root

This directory follows the conventional Kotlin project source layout used by the
fixture. The intentionally problematic service implementation lives below
`main/kotlin/com/example/`.
