# Kotlin Package Root

This directory is the Kotlin package root for the Detekt fixture.

The package tree below mirrors a normal Kotlin layout while keeping the sample
small enough to inspect by hand.
