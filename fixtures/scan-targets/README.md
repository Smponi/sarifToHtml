# Scan Targets

`scan-targets` contains intentionally flawed projects and configuration files for
SARIF-producing tools.

The targets are used by `scripts/generate-sarif-fixtures.sh` and the Docker
tooling under `tools/sarif-fixtures/` to refresh reports in `reports/`.
