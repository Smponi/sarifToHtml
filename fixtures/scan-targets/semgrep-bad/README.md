# Semgrep Fixture

This directory contains deliberately unsafe JavaScript and Python examples plus
package metadata for Semgrep SARIF generation.

The examples include obvious injection, unsafe deserialization, and secret-like
values so rules can produce stable findings.
